{
  "name": "@eventpipe/cli",
  "version": "0.2.6",
  "description": "Build and publish Event Pipe flow bundles from the terminal (session after login)",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "eventpipe": "./dist/cli.js",
    "eventpipe-cli": "./dist/cli.js"
  },
  "files": [
    "dist",
    "examples",
    "install",
    "skills",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "pnpm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "esbuild": "^0.25.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
